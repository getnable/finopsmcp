# nable

Ask Claude about your cloud and AI bill.

This package is a brand alias: `uvx nable` runs the same entry point as
`uvx finops-mcp`. In a terminal it opens the onboarding wizard; under an MCP
client it serves tools over stdio. See https://getnable.com/docs.
